#include "PixmapImage.h"

/*PixmapImage::PixmapImage(QQuickItem *parent)
    : QQuickPaintedItem{parent}
{

}

void PixmapImage::paint(QPainter* painter)
{
    painter->drawPixmap(0, 0, width(), height(), dicomImage.getPixmap());
}

 void PixmapImage::setImage(QObject* image)
 {
    QMLDicomImage* dicom = qobject_cast<QMLDicomImage*>(image);
    Q_ASSERT(dicom);
    dicomImage.SetPixmap(dicom->getPixmap());
    update();
 }*/

